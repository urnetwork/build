- Harden Android egress probe cancellation
- Points leaderboard: scrolling does not retry a failed page (the loading flip re-ran the load-more rule against the error, a hot retry loop); the footer's Try again is the retry

- Keep browser device RPC grids stable
- Points leaderboard view controller: claim the in-flight page in the scope that checks it, drop pages after close, state the goroutine-safety contract
- Expose contract sender role for lifecycle diagnostics
