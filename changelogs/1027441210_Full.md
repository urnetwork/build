## Changes in v2026.8.23-1027441210

`v2026.8.22-1026600400` ... `v2026.8.23-1027441210` -- 38 commits across 6 components.

### android

urnetwork/android [`76b0042d3...299da9d88`](https://github.com/urnetwork/android/compare/76b0042d30c4b91a474537b5ddb1bfaabfe0b89a...299da9d88f07d35e62780a7e19fdcb84c63ebb4a)

- **Add streamlined memory validation** ([`299da9d88`](https://github.com/urnetwork/android/commit/299da9d88f07d35e62780a7e19fdcb84c63ebb4a))
- **Harden Android build toolchain setup** ([`87eb47750`](https://github.com/urnetwork/android/commit/87eb47750af025344cb9fe288b03b6f0541bbc1f))

### linux

urnetwork/linux [`16596995a...f41811dcd`](https://github.com/urnetwork/linux/compare/16596995aa1db5041ad804de5a3306893d4ab48f...f41811dcdb5be482ee8ae00c1f43136102a52389)

- **flatpak: disable cairomm maintainer mode for the git source** ([`995e1de4f`](https://github.com/urnetwork/linux/commit/995e1de4fe968645c068c81d060a8a35ae5dfd68))
  Building cairomm from git rather than a release tarball turns maintainer mode on, which hard-requires mm-common-get, which the GNOME SDK does not ship:
- **flatpak: take cairomm from upstream git, not cairographics.org** ([`241b3c43b`](https://github.com/urnetwork/linux/commit/241b3c43b6edfb75acd8dcb42080fcb3e872bb1a))
  cairomm is the only module in this manifest whose source is not reachable from GitHub's runners, and it took the Flatpak build test down every time it ran:

### build

urnetwork/build [`c8c3d85d1...6abc55ddd`](https://github.com/urnetwork/build/compare/c8c3d85d101f4c724669299ee918d493c98f541c...6abc55ddd9aa04f8c813dc2c67904e938806be28)

- **ci: build and test main on push** ([`7741d3a94`](https://github.com/urnetwork/build/commit/7741d3a941d0b93fe2fea147dd795d2819b45a6f))
  main was the only branch nothing built. All five platform workflows already ran on pull_request against main, but their push triggers covered feature branches only -- all-platform-ci, upstream/all-platform-ci, linux-release-matrix -- and all-platforms.yml is workflow_dispatch. So a commit that reached main without a pull request was never built by anything.
- **linux: bump the pin to pick up the cairomm git source** ([`6093e43c3`](https://github.com/urnetwork/build/commit/6093e43c3e890d2bdc970c8b502cfb60d4ec382e))
  urnetwork/linux#11 moved cairomm off www.cairographics.org, which is not reachable from GitHub's runners and took the Flatpak build test down on four attempts across three runs. Without this bump the flatpak job here still builds the old manifest and still fails on that download, because it builds whatever this pin points at.
- **linux-build: cache the flatpak source downloads, and retry once** ([`75bd05e2d`](https://github.com/urnetwork/build/commit/75bd05e2dc89a0bd67f1e3f36ccae50405ff55a3))
  The first always-on run of this job failed, and not on anything this repo owns:
- **linux-build: run the Flatpak build test by default** ([`fdf1e6b1d`](https://github.com/urnetwork/build/commit/fdf1e6b1dfdb18f8b58f383007e2168a73bf3595))
  It was opt-in behind `flatpak: true`, on the theory that a ~2 GB org.gnome.Sdk download was too expensive to repeat on every push. Actions is free on public repositories and every urnetwork repo is public, so there was no bill to avoid. The reasoning was wrong, not merely conservative.
- **android: stop leaving main pinned to the degoogled tree** ([`ac4934e1c`](https://github.com/urnetwork/build/commit/ac4934e1c860e241c96fc52d536938c6f44f47e2))
  Every release ends with `builder build ungoogle`, which commits the build repo while the android submodule is sitting on its degoogled branch. main is then pinned to a tree whose build.gradle has every play-services dependency commented out, while the play and solana_dapp sources still import GoogleSignIn, LocationServices, ConnectionResult and friends. Those flavors cannot compile, so the android job fails on main and on every pull request opened against it, until the next release moves ...
- **linux-build: install elfutils for the native flatpak-builder** ([`8a6dba1fb`](https://github.com/urnetwork/build/commit/8a6dba1fb16e74ba579110865c09a395ed150089))
  Second failure on this job:
- **linux-build: use a native flatpak-builder in CI, and install the runtime visibly** ([`fc7cbc8e4`](https://github.com/urnetwork/build/commit/fc7cbc8e4319897983794e50611eb2b4e6ae4878))
  The first run of this job failed:
- **linux-build: group the step-summary redirects (SC2129)** ([`d3d4ea58b`](https://github.com/urnetwork/build/commit/d3d4ea58b9481fcad1bf2fbe38fb8b84e7f33832))
  actionlint runs shellcheck over every run: block, and three consecutive appends to $GITHUB_STEP_SUMMARY trip SC2129.
- **linux: ship the Flatpak from the release pipeline** ([`5699dc845`](https://github.com/urnetwork/build/commit/5699dc8459f5cd1dc8896a4dab8080351764c30f))
  The linux repo has had packaging/make-flatpak.sh and the manifest since the flatpak channel merged, and the release tag v2026.8.22-1026185650 pins a linux ref that contains both. Nothing in this repo ever called them: `make-flatpak` appeared zero times under all/ and .github/, build-arch.sh documents five artifact types and none of them is a Flatpak, and run.sh's upload glob had no *.flatpak in it. So every release shipped the Linux GUI through exactly one channel, the AppImage, while the ...

### sdk

urnetwork/sdk [`f5ed2d471...49f756fc5`](https://github.com/urnetwork/sdk/compare/f5ed2d471a5382de62e3d46a070b5c873b48798b...49f756fc5882ebf00c732284f40614bd34ff9b02)

- **Enforce 20 MiB mobile memory profile** ([`49f756fc5`](https://github.com/urnetwork/sdk/commit/49f756fc5882ebf00c732284f40614bd34ff9b02))
- **sdk: keep the identicon raster stack out of the iOS extension** ([`dddaa7899`](https://github.com/urnetwork/sdk/commit/dddaa7899f4b228cbd72d7152851a822dc65d3ad))
  RenderIdenticonPng is three lines, and it is the only importer of goidenticons in this module. Nothing else here imports image/png or golang.org/x/image. So that one exported symbol is what drags the whole raster closure -- goidenticons, x/image/{draw,vector,math}, image/png, image/draw, compress/zlib, hash/adler32 -- into every binary that links the sdk.

### connect

urnetwork/connect [`d009b2bf7...8cc216956`](https://github.com/urnetwork/connect/compare/d009b2bf7e2b4c1fdd10264c8970c95d518bd376...8cc2169565b2bc23a906943e213065f375fdadf5)

- **Bound mobile connection memory** ([`3a495e7ca`](https://github.com/urnetwork/connect/commit/3a495e7ca9c66abe34f1730b5e5f8c8319f812bf))
- **Document 20 MiB mobile memory analysis** ([`f4d36564e`](https://github.com/urnetwork/connect/commit/f4d36564ea1dc751e1370415f6899afce9d2293d))
- **Allocate message pool capacity lazily** ([`c1d9ab46d`](https://github.com/urnetwork/connect/commit/c1d9ab46deb03aed9251229ada5f26aee9e8a4ce))

### server

urnetwork/server [`17b60fe0b...1806bbc9b`](https://github.com/urnetwork/server/compare/17b60fe0b87d982928bc145e3c864793e7cfacdb...1806bbc9b1e7ed398f2541fa73fbd11019182e7a)

- **Document mobile pool performance validation** ([`1806bbc9b`](https://github.com/urnetwork/server/commit/1806bbc9b1e7ed398f2541fa73fbd11019182e7a))
- **Record message pool server performance validation** ([`1e39abccd`](https://github.com/urnetwork/server/commit/1e39abccd26363f70e73edcdbd9175463c45be34))
- **Test SSO audience allowlists** ([`38080146c`](https://github.com/urnetwork/server/commit/38080146c453d622062b6cef842001c92bf13803))
- **docs(session): correct why a wedged proxy keeps reporting under a flood** ([`0ca264d67`](https://github.com/urnetwork/server/commit/0ca264d67eab51c148f03cf975a0827c104bcd5f))
  The comment claimed the wedged proxy 'wins the token on nearly every pass' and cited TestGenuineReportSurvivesAFloodOfDistinctPeers as proof. Review found both halves false: deleting the entire global-token branch left that test green, so it never reaches the tier it was cited for, and re-running the scenario with the flood arriving first after each interval boundary named the wedged proxy 0 times out of 5, not 5.
- **test: make the flood in the liveness test four thousand real /29s** ([`ec6329b9a`](https://github.com/urnetwork/server/commit/ec6329b9a9ac4d5ac37713e1e4e1b1b374982e80))
  TestGenuineReportSurvivesAFloodOfDistinctPeers drove 4000 iterations per interval but only ~500 distinct /29s, because the counter varied bits the /29 mask keeps rather than the ones it does not -- so the bucketing the test sits next to was doing part of the work the flood was supposed to do.
- **test: say that the two report fixtures straddle a /29 on purpose** ([`64e91f64e`](https://github.com/urnetwork/server/commit/64e91f64e28adbe57566bde8924937fd702ee603))
  TestUnenumeratedProxyReportIsRateLimited proves a second peer gets its own report line using 172.18.0.7 and 172.18.0.8. Now that the suppression key is a peer network, those two are distinct only because .7 is the last address of 172.18.0.0/29 and .8 is the first of 172.18.0.8/29 -- move either and the assertion silently becomes one peer reported twice, which the interval already forbids, and it passes for the wrong reason.
- **test: pin the Retry-After VALUE, not just that one arrives** ([`4f4e8a1de`](https://github.com/urnetwork/server/commit/4f4e8a1def43abe8551fa704a0209f8009627a5f))
  CheckNetworkCreateRateLimit computes the hint in SQL and the comment above it promises "the real remaining time on the window: the oldest attempt still counted expires then." Nothing asserted the value. TestAccountLimitRefusalIsHonestAboutItsScope only required 0 \< seconds \<= window, api/handlers' assertRateLimitBody only required that the header parses and is positive, and the router tests use a test-local error type rather than model.rateLimitError -- so a flat restatement of the window ...
- **fix: key the operator report on a peer network, and say why it logs a raw ip** ([`e3c915fd2`](https://github.com/urnetwork/server/commit/e3c915fd24a6b6c1ae5fde3f4e211dd79e37d008))
  The report suppression key unmapped the peer but did not mask it, so a caller bought a fresh report slot with every source address it owned. Measured on the shipped code: one unenumerated-proxy report is 1754 bytes, and a caller rotating addresses out of ONE ipv6 /64 -- an ordinary residential allocation -- sustained the full proxyReportedMax cap.
- **fix: a forwarding header that names no client still contradicts one that does** ([`d90943e4e`](https://github.com/urnetwork/server/commit/d90943e4e85ce2f06dd26e57b5ab6ab68dbedb17))
  A client behind a trusted proxy could still choose its own client address, and therefore its own rate-limit bucket, on either deployment shape.
- **fix(test): /verify precedence pinned the forgery too, and pin the empty element** ([`46e811e24`](https://github.com/urnetwork/server/commit/46e811e240ff04df859b52c80ee4ae8a2657204c))
  Two follow-ups the full suite and a break turned up.
- **test: pin the header contract as an enumeration, not as three names** ([`be034984e`](https://github.com/urnetwork/server/commit/be034984e1da55a1197c8581c1afd309bc531d06))
  Caught by a deliberate break: gutting proxyForwardingHeaderContract down to two of the three headers left every assertion in the previous commit passing. Both reports already name all three headers incidentally -- "optionally with X-Forwarded-Source-Port", "or X-UR-Forwarded-For as ip:port" -- so Contains(report, "X-Forwarded-Source-Port") was satisfied by prose that says the opposite of what the assertion is for.
- **docs: record the two deploy-time bucket changes at the lines that cause them** ([`829c68b81`](https://github.com/urnetwork/server/commit/829c68b817e2d92c136e0aa0d6ae929cdd71354c))
  Comments only, no behaviour change. Both are things review found that are correct as written but change what a bucket means on the day this ships, and neither was stated where the code does it.
- **fix: refuse two disagreeing forwarding headers instead of letting the client pick one** ([`68403b42a`](https://github.com/urnetwork/server/commit/68403b42a0bd40cdfd858f95a6a393df2cf198b7))
  ResolveClientAddress read the FIRST forwarding header it found and ignored the rest, and forwardingHeaders lists X-UR-Forwarded-For first. A proxy overwrites the header IT sets and passes every other client header through untouched, so on any deployment whose proxy owns X-Forwarded-For -- Caddy, nginx's documented recipe, ALB, CloudFront, Cloudflare -- a client that sent its own X-UR-Forwarded-For beat the address the proxy had vouched for and chose its own rate-limit bucket, stepping ...
- **fix: name the CIDR precondition, and correct an overbroad claim in ac794cfe** ([`b927f8261`](https://github.com/urnetwork/server/commit/b927f8261e8cb846a8e05c0f0ef4c516f5ac5097))
  CORRECTION to ac794cfe, which said "Nothing that used to resolve resolves differently." That is not true, and the exception is the one place this work narrows attribution rather than widening it.
- **test: /verify source-ip precedence follows the resolver, not the old 500** ([`de7409a66`](https://github.com/urnetwork/server/commit/de7409a66a2fd54a2c0202078a8092ce3a5b148e))
  TestVerifySourceIpPrecedence case 3 asserted that a trusted proxy sending a bare X-Forwarded-For is REJECTED, on the reasoning that rejecting is safer than silently re-attributing the request to the proxy. The premise was right; the conclusion was not. The rejection is an error, and router.wrap turns that into HTTP 500 on every endpoint, so it meant the api was down for any deployment that enumerated a stock nginx.
- **fix: keep the report prune amortized, and pin the all-trusted chain** ([`6b4709874`](https://github.com/urnetwork/server/commit/6b4709874195651de60007ac32d85073ce561b98))
  Two gaps in the previous commit.
- **test: pin that a v4-mapped peer is one address everywhere** ([`68ad8f131`](https://github.com/urnetwork/server/commit/68ad8f131d9b758eb245d0d94173b80ed1919f12))
  proxyIsTrusted unmaps and server.ClientIpHashForAddr does not, so a peer left in its [::ffff:a.b.c.d] form buckets under the ipv6 /56 rule instead of the ipv4 /29 one and holds a second, separate per-address budget.
- **refactor: one tagImplError instead of three copies, so a test can pin it** ([`3b36be84e`](https://github.com/urnetwork/server/commit/3b36be84e5d992b5d6ce0c7ad5a10bb46fe20101))
  The %w that keeps Retry-After reachable through errors.As was written three times. A test that constructs its own fmt.Errorf("[impl]%w", ...) passes no matter what the wrappers do -- the first version of the test for this did exactly that and stayed green when the production tagging was reverted to %s. One function, driven end to end through WrapNoAuth and directly for the two wrappers that need a JWT, actually fails.
- **fix: read the forwarding headers real proxies send, instead of 500ing** ([`ac794cfe5`](https://github.com/urnetwork/server/commit/ac794cfe56239ffc2981eafea8f937edfa2b9d5d))
  The previous commit made an unenumerated ingress proxy loud: a request whose TCP peer is not in BRINGYOUR_TRUSTED_PROXY_CIDRS but which carries a forwarding header now tells the operator to add that peer's subnet. Review found that the remedy it printed was a way to take the api down.
- **fix: stop wrongly refusing first-time signups, and make the cause loud** ([`6a86cecd0`](https://github.com/urnetwork/server/commit/6a86cecd004c66b73d8a0e1a377bea6cc89b409a))
  A first POST /auth/network-create of the day, from an address that had created one account in ten days, was answered 429 on both production deployments. Signup was effectively down for new users.

---

_Filtered out of this changelog: 9 merge commits (both sides are already listed), 1 version-stamp commit written by all/run.sh on every release, 1 ungoogle re-tag commit written by all/run.sh. Re-run `all/changelog.py --from v2026.8.22-1026600400 --to v2026.8.23-1027441210 --filters=` to include them._
