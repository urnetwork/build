## Changes in v2026.8.22-1026185650

`v2026.8.21-1025763520` ... `v2026.8.22-1026185650` -- 31 commits across 10 components.

### apple

urnetwork/apple [`d2567e195...9a8a7b22d`](https://github.com/urnetwork/apple/compare/d2567e1953f018d46d4f9c3ab66b06b6a2058f94...9a8a7b22de15eda9fb8734256f4b32ddaafc759d)

- **tests: the selectable transport order is H1-first, matching the SDK** ([`6a4080625`](https://github.com/urnetwork/apple/commit/6a4080625bb5654ed4f71ab25cdad3e8470b38fa))
  TransportStatsTests asserted [.h3, .h1, .dns, .dnsPump] in four places that read their value from the SDK. The SDK's order is H1-first, and it is explicit about it:
- **ci: build and test the apple app on every push** ([`e5e66859b`](https://github.com/urnetwork/apple/commit/e5e66859b7a2d7d23d464d3eda435c7b9a4e0001))
  This repo had no .github directory at all, so nothing verified that the iOS and macOS apps still compile or that the unit suite still passes. The release pipeline (build/all/run.sh) archives and signs; it never runs networkTests. This closes that gap.

### extension

urnetwork/extension [`8f75f07d8...660e05e39`](https://github.com/urnetwork/extension/compare/8f75f07d8bd0fdfea983f8ed9453807914f25177...660e05e39d965e43e3c27ca099cd167376ba6fce)

- **fix(types): replace the nine `any` casts and drop an unused listener param** ([`fbc4ec63f`](https://github.com/urnetwork/extension/commit/fbc4ec63f993fc18d5aaaa3b45bdfdeac5eaf605))
  Pure typing/style debt, no behaviour change -- `as any` erases at compile time and the one runtime edit (dropping a trailing listener parameter) is invisible to the caller.
- **fix(connect): stop writing the SDK callback refs during render** ([`4e1c494c7`](https://github.com/urnetwork/extension/commit/4e1c494c7a26e5330f5692255419e1a68b46a995))
  useConnectionManager kept the latest authNetworkClient/removeNetworkClient in refs with bare assignments in the hook body:
- **ci: add timeout and concurrency; correct the lint comment** ([`c793894d5`](https://github.com/urnetwork/extension/commit/c793894d5f436038739a17bf0b87b11c3d951913))
  Two things the workflow was missing and two the comment got wrong.
- **ci: report lint without gating build and test** ([`0f912dec0`](https://github.com/urnetwork/extension/commit/0f912dec04464e80c29a3bf41515669147ffdebf))
  `eslint .` fails on a fresh checkout of main with 12 pre-existing errors (10 no-explicit-any, one unused binding, two react-hooks ref-during-render in elements/src), which stopped the run before the tests or either build ran at all.
- **ci: build and test the extension on push** ([`9f4d6fdf6`](https://github.com/urnetwork/extension/commit/9f4d6fdf6bdddb7ad7dd7903fa31157c167a5bd6))
  Adds the repo's first CI. Runs the checks the repo already defines -- eslint, the tsconfig.test.json type-check, and the vitest suite -- then builds both store targets the way the Makefile does (npm run build, npm run build:firefox) and asserts each produced a real zip.

### build

urnetwork/build [`4de28e9dd...8c422231f`](https://github.com/urnetwork/build/compare/4de28e9dd0ec993f9dae92d9934f5f88d02572c9...8c422231f84a3413d16d0466a66141edad18f575)

- **changelog: one directory, one file per audience, per release** ([`7ee456401`](https://github.com/urnetwork/build/commit/7ee456401ca6a18e28d644d13808c12b42a2fef7))
  The previous commit gave apple, windows and linux a note of their own but scattered them into fastlane-shaped paths that each release overwrote, and left the full changelog inlined in the release body. This puts all six files in one place and keeps every release's set.
- **changelog: one release note per storefront, app changes first** ([`785a706fe`](https://github.com/urnetwork/build/commit/785a706fec47d71ea726fdab4782fb61c1860862))
  There is no such thing as "the store note". Four storefronts serve four different artifacts and not one of the three numbers that matter -- the limit, the format, the field -- is shared between them:
- **extension: bump the submodule past the lint fixes** ([`57ed7d8c2`](https://github.com/urnetwork/build/commit/57ed7d8c2ea5b42eedc79b21fd482095f8af2fbf))
  The extension leg of the CI has been red since it was added, and it is not the workflow's fault: this repo pinned extension at 8f75f07d8b, which predates urnetwork/extension#36. `npm run lint` fails there with 12 errors -- 9 no-explicit-any, an unused parameter, and two react-hooks/refs violations, one of which was a real correctness bug (refs assigned during render, feeding a ConnectionManager built once with [] deps that invokes the auth thunk from timers).
- **changelog: say so when running anonymously degrades the store note** ([`25f5f0d8e`](https://github.com/urnetwork/build/commit/25f5f0d8e9f2e56f124c1eaef9293f7eb2a56d70))
  The store note holds CI/test/docs-only commits back by looking up which files each candidate commit touched. That is one extra API call per candidate, and anonymous callers get 60 requests/hour -- so on any real span the lookups start returning 403. commit_files() then fails OPEN by design, because a missed lookup must never delete somebody's work from the release notes, which means the commit is KEPT.
- **changelog: generate release notes from the commits instead of copying a placeholder** ([`1e816e157`](https://github.com/urnetwork/build/commit/1e816e15793a59581c48c50c5eec21a32b38bd38))
  Every changelog this repo has published says the same thing. all/run.sh copied metadata/en-US/changelogs/pending.txt verbatim into metadata/en-US/changelogs/\<version code\>.txt on every release, pending.txt is a static one-liner, and so 1025763520, 1025613560, 1025339670 and 1025223880 are byte-identical: "- Bug and performance fixes."

### sdk

urnetwork/sdk [`e3c36a9ba...3739dcfac`](https://github.com/urnetwork/sdk/compare/e3c36a9ba6613da10eeff776ec306132d3a7310b...3739dcfac58a701891e843c9bd1c65c42c7ade88)

- **Raise iOS extension SDK size limit to 60 MiB** ([`3739dcfac`](https://github.com/urnetwork/sdk/commit/3739dcfac58a701891e843c9bd1c65c42c7ade88))
- **sdk: keep runtime/pprof out of the iOS builds** ([`497d82552`](https://github.com/urnetwork/sdk/commit/497d8255259d498366bb69b64a0320d488eb4d95))
  The iOS extension SDK is over its size budget and cannot simply be raised past it:

### warp

urnetwork/warp [`678b25a8b...e13673c63`](https://github.com/urnetwork/warp/compare/678b25a8bbd2e564cc9d8fa954322b99af6bba0e...e13673c63a88efcebd0716d67d32f3d387343ac8)

- **ci: bound the job, cross-compile the shipped platforms, race the suite** ([`682388b50`](https://github.com/urnetwork/warp/commit/682388b50ebc472873a356c247eb12b25d7e561a))
  Follow-up on the same workflow. Five additions, all verified against a clean ubuntu:24.04 before pushing:
- **ci: quarantine TestNginxLogsOmitClientAddr, which fails on ubuntu-latest** ([`a2b0a0503`](https://github.com/urnetwork/warp/commit/a2b0a0503159b4f42619e1498add1d9bea9b7482))
  The first run of this workflow went red, and the cause was an assumption in the previous commit that CI just disproved: that warp's nginx tests would skip on a runner. One of them does not.
- **ci: build and test warp on every push** ([`afb3f80ef`](https://github.com/urnetwork/warp/commit/afb3f80efbe66eced82dbcbcafd75c42fc517bb9))
  warp had no CI. This adds the same build-and-test job the other urnetwork Go repos run, so a push or PR compiles every package and runs the real test suite rather than nothing.

### server

urnetwork/server [`66af1304a...2c9e92b45`](https://github.com/urnetwork/server/compare/66af1304a2fdcbd4c850c9c906a1def8217472a3...2c9e92b45073421617dc2bc13b61778a42ff06c4)

- **feat(model,api,taskworker): bootstrap the egress prober's credential without a human** ([`a2731f132`](https://github.com/urnetwork/server/commit/a2731f132170f40eb53fc8d955618e03c2f78a2d))
  A deployment that had never had an operator hand-mint a prober credential had no egress probing at all, and said nothing about it. The steps were: create a network, POST /network/auth-client, paste the by_client_jwt into the prober's environment. Miss them and every provider goes unprobed, which is indistinguishable from a fleet that is simply healthy -- the failure is silent in exactly the way that matters.
- **feat(model,api): detect providers that go dark, separately from egress health** ([`ed51f6d60`](https://github.com/urnetwork/server/commit/ed51f6d60fc1f5f801c56643b952f44b8b218ffb))
  Egress health is the wrong instrument for this question. It samples ~131 destinations across several classes and takes minutes per provider, so it can only sweep the fleet slowly -- measured on beta, a provider went days between measurements. For that whole window a provider that has stopped carrying traffic is invisible: it stays connected, keeps accepting clients, keeps being advertised, and answers nothing. Making health cheaper is not the fix; the sampling breadth is what makes it worth ...
- **test(model): pin the egress-health age bound that stopped advertising blackholes** ([`8debc157b`](https://github.com/urnetwork/server/commit/8debc157b231131d5d263ae0bf6a5488c2b68ed7))
  The bound landed with the fix; nothing pinned it. That is the gap worth closing, because the behaviour it enforces is a NEGATIVE one -- a provider disappearing from the map -- and a bound that silently stops applying looks exactly like a fleet that happens to be healthy.
- **feat(model): make the active bandwidth probe budget a deployment setting** ([`d3e5a95c2`](https://github.com/urnetwork/server/commit/d3e5a95c257b6e47936de4b94cae9198b7fbd4c1))
  The hourly probe budget is CAPACITY, and capacity is a property of the deployment rather than of this code. A 40-provider fleet on a 4-core box and a production network carrying ~100k providers cannot share one compiled-in number: each has to measure its own limits and set its own. That is the opposite of a behavioural knob like the sampling rate, which stays one value everywhere precisely so every deployment exercises the same path.
- **feat(model): name the prober as the fault when the whole fleet fails identically** ([`b6aee48a2`](https://github.com/urnetwork/server/commit/b6aee48a21b7e68e64c24b28a5314107b49e647f))
  A prober whose platform jwt had been rejected reported EVERY provider as no_consensus for eight hours. Every individual record was well-formed, every endpoint returned 200, the due queue kept handing out work and the prober kept taking it. Per provider the data was indistinguishable from a genuinely unreachable fleet, and nothing anywhere said "credential".

### proxy

urnetwork/proxy [`bb5794710...3e94ca0f2`](https://github.com/urnetwork/proxy/compare/bb579471068b4493f17fdacc6062f2cfb385017f...3e94ca0f2c4b235b0d4f8b2a71a25a74ae4195f6)

- **ci: add a concurrency group and a vet step** ([`52910aebc`](https://github.com/urnetwork/proxy/commit/52910aebcd1c511ea450f544e82de4d4a501ace9))
  Two house conventions the first commit left out.
- **ci: build and test proxy on every push** ([`f76ff29f4`](https://github.com/urnetwork/proxy/commit/f76ff29f4effcc6aa9c618fa7156a3ee0a259f06))
  proxy had no CI. This adds the same build-and-test job the other urnetwork Go repos run, so a push or PR compiles every package and runs the real test suite rather than nothing.

### localizations

urnetwork/localizations [`e4715b9f2...d64c666e2`](https://github.com/urnetwork/localizations/compare/e4715b9f24eec7793b21f17a5d4e103092fb509f...d64c666e2cf9a3e7ee31482487c36dcd043ba896)

- **ci: build and test the localization store on every push** ([`bd3fb6fd5`](https://github.com/urnetwork/localizations/commit/bd3fb6fd529dc3e9de745f06db515c5750d76326))
  This repo had no CI. Nothing validated keys/*.yaml until the release build ran `npm run gen` and wrote into the app repos, so a malformed key -- the common failure when a translation lands -- surfaced far from where it was introduced.

### glog

urnetwork/glog [`45662c0f2...93fe11b3e`](https://github.com/urnetwork/glog/compare/45662c0f2b08317b399b9f721483c673e49bac8a...93fe11b3e490abde48763b2dd0cc4e9b1e282ab6)

- **ci: record the TestRollover flake this workflow surfaced** ([`ed159a35e`](https://github.com/urnetwork/glog/commit/ed159a35ece7cfcb1818a24f1b9db09090dbae22))
  The first CI run on this branch went red on TestRollover, then a re-run of the same commit went green. Root cause, reproduced locally 50k+ times and independent of CI:
- **ci: add concurrency group and timeout to the glog workflow** ([`36e3f1128`](https://github.com/urnetwork/glog/commit/36e3f1128fb43862d30735255bbb4482b6805d60))
  The previous commit renamed test.yml to build-and-test.yml but landed only the rename; this carries the content change that was meant to go with it.
- **ci: build and test glog on every push** ([`1ee639974`](https://github.com/urnetwork/glog/commit/1ee6399749d72b5c832d9c4ef3828aff19048b4b))
  glog had no CI of its own. This adds a build-and-test workflow so a push to master, a PR against master, or a manual dispatch compiles the package and runs the real test suite.
- **ci: build and test glog on every push** ([`ec233ad58`](https://github.com/urnetwork/glog/commit/ec233ad589e13cc4e242cad9ab5bdd805e4df5c2))
  glog had no CI. This adds the same build-and-test job the other urnetwork Go repos run, so a push or PR compiles the package and runs the real test suite rather than nothing.

### goidenticons

urnetwork/goidenticons [`408ab24fc...325750b38`](https://github.com/urnetwork/goidenticons/compare/408ab24fc36531e97a0cd04f31a42796d151fa98...325750b38314313dc5f44c880ab6f12f6c1ecb3c)

- **ci: bound the run and vet, and name the file for what it does** ([`4a65af535`](https://github.com/urnetwork/goidenticons/commit/4a65af5352365bcbbb0a3ec80c0f2fc52eca1711))
  Follow-up on the same branch, aligning this workflow with the newest house convention (server/test.yml, linux/build.yml) rather than the oldest (connect/test.yml, sdk/test.yml, which predate all three of these habits):
- **ci: build and test goidenticons on every push** ([`9293c1409`](https://github.com/urnetwork/goidenticons/commit/9293c1409060c316d97d6eea766f8459f4a4148f))
  goidenticons had no CI. This adds the same build-and-test job the other urnetwork Go repos run, so a push or PR compiles the package and runs the real test suite rather than nothing.

---

_Filtered out of this changelog: 18 merge commits (both sides are already listed), 1 version-stamp commit written by all/run.sh on every release. Re-run `all/changelog.py --from v2026.8.21-1025763520 --to v2026.8.22-1026185650 --filters=` to include them._
