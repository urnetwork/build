## Changes in v2026.9.4-1037574650

`v2026.9.4-1037517570` ... `v2026.9.4-1037574650` -- 74 commits across 11 components.

### android

urnetwork/android [`d27bfd916...7f4af2a21`](https://github.com/urnetwork/android/compare/d27bfd9161cdb372a6ead0f90383dabc53294cf0...7f4af2a219310a3294b0be90e08ea1536b478049)

- **Instant account screens read their strings from the store** ([`7f4af2a21`](https://github.com/urnetwork/android/commit/7f4af2a219310a3294b0be90e08ea1536b478049))
  CreateNetworkInstant and SeedphraseDisplayScreen carried hardcoded copy; use the shared keys and the five new ones, with the regenerated catalogs.
- **Always show both plans on the Play upgrade picker** ([`7a77b3734`](https://github.com/urnetwork/android/commit/7a77b373460a82a7df96fea1dc83ddef7678858d))
  On Play the yearly plan and its 14-day trial are assumed to exist: the picker renders yearly (selected) and monthly unconditionally, before, during and after the store query, using the shared fallback prices until Play's offers refine them. A build the store does not recognise used to degrade to a monthly-only screen. The purchase no longer falls back to the other plan's offer when the picked plan has none; the store error surfaces instead. The offers query still retries and logs its final ...
- **Make the seed phrase copy button a secondary action** ([`f607f5d76`](https://github.com/urnetwork/android/commit/f607f5d760645963951b97a96da118cdee4ba61e))
  The seed phrase screen showed two primary buttons. Copy is now an outlined button with a copy icon, labelled from the store, so the confirmation is the screen's one primary button.
- **Run Android acceptance across the eligible device fleet** ([`90e11c66b`](https://github.com/urnetwork/android/commit/90e11c66baf2d61d8fbe567d63a1708f897f1917))
- **Retry the Play offers query and repeat it when the plan picker opens** ([`fff168373`](https://github.com/urnetwork/android/commit/fff1683738d3c8d0c9bae05df84378f5085a2ba9))
  The store offers were queried once at launch; when Play answered SERVICE_UNAVAILABLE (which it does while the app's own tunnel is coming up) nothing retried and the picker showed only the monthly plan for the rest of the session. Retry with exponential backoff, log the response code of a failed query, and query again when the upgrade screen opens.
- **Drop the Widgets row from Settings** ([`b1501e473`](https://github.com/urnetwork/android/commit/b1501e473d5548a3c94db4d44a79e8485b36f697))
  Widgets is a top-level Account section, so the Settings link duplicated it.
- **Label the account switcher's referral rows from the store** ([`825f313fd`](https://github.com/urnetwork/android/commit/825f313fdf313f6aa19cacf6e148d6ec5b67e115))
  The rows open the Referrals screen now, so they use the Referrals key and icon that the Account section uses instead of a hardcoded share label.
- **Open the one Referrals screen from every referral entry point** ([`029f2026f`](https://github.com/urnetwork/android/commit/029f2026f0515e1218dbe94c235da4c8cde05168))
  The connect drawer's referral row, the account switcher's share rows and the referral toast opened the old refer overlay, a second referral design. They now navigate to the Account \> Referrals screen, and the overlay is removed.
- **Connect drawer Get Pro opens the upgrade flow** ([`3b7bacf88`](https://github.com/urnetwork/android/commit/3b7bacf88b53fde3d61ec8322314728321ecde44))
  The drawer's Get Pro launched the onboarding funnel. It now navigates to the same Upgrade route Account's Change opens, and the unused launchIntro callback chain through ConnectScreen and MainNavContent is removed.
- **Keep the intro funnel from returning after rotation** ([`2711a386b`](https://github.com/urnetwork/android/commit/2711a386b00ea7fa14effdce9b66a12bcab08abb))
  The nav view model captured the SDK can-prompt flag once at creation and never updated it when the funnel was presented, so a recreated activity re-ran the presenting effect against the stale armed snapshot and showed a completed funnel again. An IntroFunnelGate now mirrors the flag and drops it synchronously when the funnel is marked prompted; the device manager persists the flag itself instead of the legacy last-prompted timestamp that re-armed the funnel after five days.
- **Points leaderboard: network name above the emoji tag in rows and the own card** ([`f0873ff25`](https://github.com/urnetwork/android/commit/f0873ff25bb875a2d74b7a29fd30309d05645e80))
- **Points leaderboard rows: emoji tag on its own line above the network name** ([`6d14e6b9f`](https://github.com/urnetwork/android/commit/6d14e6b9f403ed360d430bba60d6987481fd8b71))
  On narrow screens a four-emoji tag left the name column no room, so Anonymous was truncated to three letters. Stack the tag over the name like the own-stats header and let the name take the remaining width.
- **login: the instant account plays the welcome animation before entering, like every other sign-in; the sign-ins that complete in the activity (auth code link, Apple and bittensor returns) play it too** ([`758e170af`](https://github.com/urnetwork/android/commit/758e170af389d65532733aa3295cba1a3533d49c))
- **Widget preview sample: burst centers on the bucket grid, no rescale, acks at a tenth of the bytes** ([`9f42bbea0`](https://github.com/urnetwork/android/commit/9f42bbea0931b50cf9ed33b310c4f826215267a7))
- **Widget preview sample: realistic bursty client traffic, providing never** ([`64f997873`](https://github.com/urnetwork/android/commit/64f997873f020928f6a4e52b781e0a261f6a146f))
- **Points leaderboard: own name from me or the jwt, anonymous rows read Anonymous, the ranked count sits under the emoji, one continuous chunked list; switch copy is Show my network name** ([`1a0ac49f7`](https://github.com/urnetwork/android/commit/1a0ac49f7115ab4969d6fb49782e43de210aa91a))
- **Points leaderboard design: every ranked network is listed, the switch reveals the name, one continuous list in 50-row chunks** ([`feb6939f9`](https://github.com/urnetwork/android/commit/feb6939f987406da7bd20e7ed2ee5e3625800ce8))

### apple

urnetwork/apple [`3c47432ec...c81d68922`](https://github.com/urnetwork/apple/compare/3c47432ec5d0a3f367574398d23e2457872dbd1e...c81d689221e277cb107999833b8789f0a57a2a0f)

- **Always show both plans in the picker, before the store answers** ([`70173cde4`](https://github.com/urnetwork/apple/commit/70173cde4112bab5944f68bc1f25efdaa2695d29))
  The upgrade sheet and the onboarding plan step waited for StoreKit before rendering anything, so a slow or failed product fetch hid the yearly plan behind a spinner or a retry screen. The plans always exist on the App Store: render the picker unconditionally with the store list prices, let a loaded product refine its own row, and keep the trial line on yearly. A tap before the product arrives reports itself where a failed purchase does and asks the store again. The saving percent was ...
- **One primary button per screen in the sign-up flows** ([`ba39dc34b`](https://github.com/urnetwork/apple/commit/ba39dc34b407f379a700a22de4095ddc8a77aa1b))
  The seed phrase page's copy button becomes an outlined secondary button with a copy icon, and the Apply bonus button on the create-network screens sits next to the main CTA as a secondary button. UrButton gains an optional leading SF Symbol for that.
- **One referrals screen everywhere** ([`1e7000b70`](https://github.com/urnetwork/apple/commit/1e7000b70178480b111bbf0050a68775e2ff6ce8))
  The connect drawer's referral row shared the referral link and the account menu offered a bare share item, while Account had the real Referrals screen with the count tracker. The drawer row now opens that same ReferralsView in a sheet on iOS and macOS, the account usage bar row and the account menu open the Referrals section, and guests create an account first as the Referrals row already did.
- **Persist onboarding completion so a rebuilt view never re-prompts** ([`5cc6cc793`](https://github.com/urnetwork/apple/commit/5cc6cc7932c6c1a37e231c4c0ffbbfafb0d23909))
  Closing the introduction only cleared the presented flag of the tab or split view; the introductionComplete binding that seeds a rebuilt view stayed false and the SDK canPromptIntroFunnel flag stayed armed, so any re-created navigation root showed the onboarding again. Finishing or skipping now marks the binding complete and writes the SDK flag through the DeviceManager before the cover or sheet goes away, the macOS sheet also finishes on a system dismissal, and the gate decision lives in a ...
- **Open the upgrade sheet from the connect drawer's Get Pro** ([`718c29da9`](https://github.com/urnetwork/apple/commit/718c29da9a8a2e2dd55c03c33cb1661239c5257b))
  The drawer's Get Pro went through promptMoreDataFlow, which shows the introduction, while Account's plan row presents the upgrade sheet. Give ConnectActions an openUpgradeSheet closure wired to the connect view's existing upgrade sheet on iOS and macOS, collapsing the drawer first on iOS. Insufficient balance keeps the participate flow.
- **Points leaderboard: network name above the emoji tag** ([`07898421d`](https://github.com/urnetwork/apple/commit/07898421df1f86abc864f270d48f0fcb3940691e))
  The ranked rows and the own-stats header now put the network name on the first line and the emoji tag on the line below it, so the name reads first on every platform.
- **Stack the emoji tag above the name in points leaderboard rows** ([`bae6ba27b`](https://github.com/urnetwork/apple/commit/bae6ba27b8ed0586f1ba024bf34830e2e37fba94))
  On a narrow screen the tag and the network name shared one line, so a four-emoji tag left the name truncated to a few characters. The identity cell is now a leading-aligned stack: tag on the first line when present, name on the line below, with the numeric columns centered against it.
- **Drop the stale 0-5 axis under the referral bar** ([`84a8b7367`](https://github.com/urnetwork/apple/commit/84a8b73676f696a210905e7a1c9c9d0b5b97c2d1))
  The bar's axis still counted to five from the first draft, when the referral cap was hardcoded to 5. The cap now comes from the referral terms and no other platform draws ticks under the bar, so render a plain slim filled bar.
- **Widget sample: snap burst peaks onto the bucket grid** ([`22360ba4a`](https://github.com/urnetwork/apple/commit/22360ba4ab9479a6b4d83c5bb65b39d7cb78c4aa))
  The sample curve's burst centers now sit exactly on sample points of the 60-bucket grid, so each peak lands in one bucket and the chart labels read the full 410 KiB/s and 594 pkt/s. Upstream is a tenth of the bytes and half the packets, matching the Android sample term for term.
- **Fix the onboarding connector mark landing below the header slot** ([`ba762af80`](https://github.com/urnetwork/apple/commit/ba762af8054cc1d25b5a14a2b02fc295186b9be9))
  The floating mark measured the header slot in a named coordinate space that is not resolvable across the NavigationStack push, so SwiftUI fell back to window space and the mark landed one safe-area inset too low. Measure in the global space, subtract the overlay's own origin, and snap to the slot once the flight has settled.
- **Widget sample: bursty client traffic curve, provide mode Never** ([`aaf3bce25`](https://github.com/urnetwork/apple/commit/aaf3bce25d2fdec1ce6761001ac2e1547b5d4dd0))
  The sample the widget gallery, placeholder and onboarding preview draw now shows an hour of realistic client traffic: a 6 KiB/s floor with six sharp bursts of different heights (the tallest still under way at the right edge), with the packet line following the same bursts at its own heights so both lines read. The curve is a deterministic sum of gaussian bursts shared term for term with the Android sample. The sample tunnel now has provide mode Never with no provider traffic, so the ...
- **Points leaderboard: the switch reveals the network name; catalog regenerated and the tab's literals follow** ([`5f1f12b39`](https://github.com/urnetwork/apple/commit/5f1f12b39ec3da4499a94a1d166c26e7ab534c27))

### linux

urnetwork/linux [`8ad7c80a1...725b4ec01`](https://github.com/urnetwork/linux/compare/8ad7c80a1c0a463b011997cd0207c7cfe5b586c6...725b4ec01b3b600e87967e276a3375fc0a0b874b)

- **Regenerate catalogs for the instant account keys** ([`725b4ec01`](https://github.com/urnetwork/linux/commit/725b4ec01b3b600e87967e276a3375fc0a0b874b))
- **Seedphrase sheet: copy is a secondary pill with a copy glyph** ([`69556c5da`](https://github.com/urnetwork/linux/commit/69556c5da6c015901366308df91d5f8a6f27d06d))
  The instant-account seedphrase sheet showed copy as a bare GTK button beside a suggested-action confirm. Copy is now a URButton secondary pill with a leading edit-copy glyph (named for a11y), and the confirm is the sheet's one primary in the same button kit. Copy still relabels itself Copied and leaves the sheet open.
- **Drawer referral row opens the Referrals page; Get Pro label key** ([`f1d1fb235`](https://github.com/urnetwork/linux/commit/f1d1fb23513a20bcb6a68b99c06ac0f161c3d4cc))
  The usage bar's "Total referrals" row in the connect drawer is now a flat button that navigates to Account \> Referrals, the one referral screen, the same as the android and apple drawer rows. The drawer's Get Pro button uses the get_pro key like every other app instead of become_supporter.
- **Do not restart an onboarding flow that is already on screen** ([`473140396`](https://github.com/urnetwork/linux/commit/4731403960b983e5fd2583808074b64e1a13c83a))
  A repeated logged-in transition re-ran OpenOnboardingIfPending, and Open() resets the flow to page 1 while the pending pref is still armed. Skip the open when the onboarding window is already visible; the persisted pref stays the only gate and is cleared when the flow hides.
- **Points leaderboard: network name above the emoji tag** ([`f8df56a30`](https://github.com/urnetwork/linux/commit/f8df56a30380b20e3fa7114081640e6968e08fe1))
  The row stack now keeps the name on the top line, which stays bound into the row's cells, with the tag beneath it hidden while empty. The own-stats header follows the same order: the name line with the pencil, the tag on its own line under it, then the ranked count.
- **Points leaderboard rows: emoji tag on its own line above the network name** ([`c86311a5e`](https://github.com/urnetwork/linux/commit/c86311a5e1ca71a2880eae94bade23bad82763b5))
  A long tag on the same line squeezed the name to a stub in a narrow pane. The identity cell is now a two-line stack (tag, then name) built by a new pane-kit helper, and the points rows are 52 px tall to fit it.
- **Points leaderboard: one continuous list that appends each chunk, never re-requests a failed page, shows the own name (from me or the jwt) on the header and the own anonymous row, ranked count on its own line under the emoji** ([`0c52e2db5`](https://github.com/urnetwork/linux/commit/0c52e2db50d33ac17ce9039fe1315e9fbfb2065a))
- **Points leaderboard: the switch reveals the network name (fallbacks and regenerated po)** ([`f6b24bf4f`](https://github.com/urnetwork/linux/commit/f6b24bf4f78453ccfa9034299fb9009b9575f3a0))

### windows

urnetwork/windows [`ad44c58c8...c0fead2e0`](https://github.com/urnetwork/windows/compare/ad44c58c836da023a5aa42fc7a12e1173068f95b...c0fead2e0a91ac9f7ddcd104f42751270c778595)

- **Regenerate strings for the instant account keys** ([`c0fead2e0`](https://github.com/urnetwork/windows/commit/c0fead2e0a91ac9f7ddcd104f42751270c778595))
- **Seedphrase sheet: copy is a secondary button with a copy glyph** ([`037c4fe2d`](https://github.com/urnetwork/windows/commit/037c4fe2d55c86a2e147ac40cb889824cf1dfd64))
  The instant-account seedphrase sheet had its copy action on the dialog's button row next to the confirm. Copy is now a plain (secondary) button with a leading copy glyph inside the content, so the confirm is the sheet's one primary action. Copy still leaves the sheet open.
- **Balance warning action uses the get_pro label like every other app** ([`68b907868`](https://github.com/urnetwork/windows/commit/68b9078684e94f686d8508a3fbb9b7e24ae5d016))
- **Points leaderboard: network name above the emoji tag** ([`33703a200`](https://github.com/urnetwork/windows/commit/33703a2004df09a77ae533b8f5a9ee9b3cd57021))
  The row stack now keeps the name on the top line, which stays bound into the row's cells, with the tag beneath it collapsed while empty. The own-stats header follows the same order: the name line with the pencil, the tag on its own line under it, then the ranked count.
- **Points leaderboard rows: emoji tag on its own line above the network name** ([`2ddbf2258`](https://github.com/urnetwork/windows/commit/2ddbf22587bfa7875524217b3afb24e174bef5fb))
  A long tag on the same line squeezed the name to a stub in a narrow window. The identity cell is now a two-line stack (tag, then name) built by a new component helper, and the points rows are 52 px tall to fit it.
- **Points leaderboard: one continuous list that appends each chunk, never re-requests a failed page, shows the own name (from me or the jwt) on the header and the own anonymous row, ranked count on its own line under the emoji** ([`8aabd7a73`](https://github.com/urnetwork/windows/commit/8aabd7a7387d09d06edeaa4795723edee0bfc33a))
- **Strings: the points leaderboard switch reveals the network name (regenerated resw)** ([`ab3649a5d`](https://github.com/urnetwork/windows/commit/ab3649a5d9f91562f69d57739c78e12138c8629f))

### build

urnetwork/build [`3395f9f5b...68eebf1b3`](https://github.com/urnetwork/build/compare/3395f9f5b407cb927c82a6e440e430fd2f2c7f4d...68eebf1b30a40b668b68fe35d45d24027da26515)

- **2026.9.4-1037517570 restore android pin** ([`68eebf1b3`](https://github.com/urnetwork/build/commit/68eebf1b30a40b668b68fe35d45d24027da26515))
- **Retry raced build main pushes** ([`8a7b7871d`](https://github.com/urnetwork/build/commit/8a7b7871d130b07d59c4750c8741dba81f9354e2))
- **Join P2P provider cleanup before logout** ([`1fefa654f`](https://github.com/urnetwork/build/commit/1fefa654f2c42bd9dc36bd668fe72a724d70dd49))

### sdk

urnetwork/sdk [`0e75cb300...e1d8dc8d9`](https://github.com/urnetwork/sdk/compare/0e75cb300b314c92931ddbaca01adfd0b5d4343e...e1d8dc8d9682daefd86878fea911b7b643634406)

- **Cancel stale JWT refresh on RPC publication** ([`e1d8dc8d9`](https://github.com/urnetwork/sdk/commit/e1d8dc8d9682daefd86878fea911b7b643634406))
- **Wake failed token refresh when remote transport activates** ([`27016a40d`](https://github.com/urnetwork/sdk/commit/27016a40df720f9dbb798ec35827674d9218d117))
- **Points leaderboard: the display name is the name the server sent, so a network sees its own name in its header while others see it as Anonymous** ([`4afff3182`](https://github.com/urnetwork/sdk/commit/4afff3182f6c91e32edb01fa47fab32b266776a9))

### connect

urnetwork/connect [`e99d1a6d8...a62f96962`](https://github.com/urnetwork/connect/compare/e99d1a6d8032a673f2a803428ae6e29014d70a40...a62f96962ffa885463535de2eb5feab2e59286b2)

- **Synchronize platform transport auth snapshots** ([`08a746146`](https://github.com/urnetwork/connect/commit/08a7461467f2356fe0dff17dc3b3e617d255bc9c))
- **test: stream filtered output promptly** ([`a5981b275`](https://github.com/urnetwork/connect/commit/a5981b275ecfd72d77e58c363adead0b71257470))
- **API spec: /network/clients lists top-level devices only; /network/proxies lists the hosted proxy devices** ([`5a1c6ab1a`](https://github.com/urnetwork/connect/commit/5a1c6ab1a42ca8362536a909aac00340f638ee90))
- **API spec: the points leaderboard lists every ranked network; points-ranking-visibility only reveals the name** ([`630fec07e`](https://github.com/urnetwork/connect/commit/630fec07ef23a357017f6d83447b2aa16f926c5a))

### warp

urnetwork/warp [`06c376a5b...f1503d6c4`](https://github.com/urnetwork/warp/compare/06c376a5ba3a1b781b09a3be9567a006136469f2...f1503d6c46f45dcf922de74909732a9c7f1553ed)

- **fix: ignore staged config versions** ([`f1503d6c4`](https://github.com/urnetwork/warp/commit/f1503d6c46f45dcf922de74909732a9c7f1553ed))
- **fix: preserve short Loki tail bursts** ([`35453fd69`](https://github.com/urnetwork/warp/commit/35453fd69d878aed12525681e6856c983e72dcb5))

### server

urnetwork/server [`4849efd38...b6ba736bd`](https://github.com/urnetwork/server/compare/4849efd38fc5d50031843f2bbb8f9d81bf5d0216...b6ba736bddaefb19e34543d2d5e6d8d56204f68a)

- **Test monitor documentation assertions** ([`b6ba736bd`](https://github.com/urnetwork/server/commit/b6ba736bddaefb19e34543d2d5e6d8d56204f68a))
- **Refine runtime and live-tail diagnostics** ([`2dc62b200`](https://github.com/urnetwork/server/commit/2dc62b2008f73df0f9549a823923dda7feffa37e))
- **Monitor migration artifacts through version 629** ([`d67cdc027`](https://github.com/urnetwork/server/commit/d67cdc02744219130bf30f4a55d15f4198d55e5b))
- **test: stream filtered output promptly** ([`2042c4c68`](https://github.com/urnetwork/server/commit/2042c4c68314dbb14fcf07d86eea5556d1295b3d))
- **Use current network name on points leaderboard** ([`3b34741ff`](https://github.com/urnetwork/server/commit/3b34741ff147b08cc000a04f147c52ef473f93c4))
- **test: close optional performance profiles** ([`4d600beac`](https://github.com/urnetwork/server/commit/4d600beacb34e668041e3f7f4b40074621361004))
- **test: constrain local performance ICE** ([`333024133`](https://github.com/urnetwork/server/commit/33302413355a9642b902d1dc8ab363ea471482ff))
- **Classify provider tunnel teardown logs** ([`46320ceb3`](https://github.com/urnetwork/server/commit/46320ceb3c8f549fbece4e1110beea9207518832))
- **Fix multi-hop contract payout allocation** ([`18fe118dd`](https://github.com/urnetwork/server/commit/18fe118dd2ac195b1081f765f6c284c24dfd9128))
- **Distinguish monitor IPv6 route loss** ([`18e12a379`](https://github.com/urnetwork/server/commit/18e12a3797d2d2f81e3cb6e5991920acd14b4992))
- **test: bypass native DNS for local service probes** ([`2591f51c8`](https://github.com/urnetwork/server/commit/2591f51c85f70b901296297a8ecf9e0d4e13204c))
- **Handle empty filtered journal results** ([`5a7bef92a`](https://github.com/urnetwork/server/commit/5a7bef92a58e614b2b15a74c3d0dc3fc7e66d39b))
- **Network clients list only the network's top-level devices; hosted proxy devices move to GET /network/proxies with their credentials** ([`0bd3b29e2`](https://github.com/urnetwork/server/commit/0bd3b29e29fe0e899bc7baf5396f0e95cbf8e69b))
- **Points leaderboard: every ranked network is listed; the Points switch reveals only the name (rows and me keyed on points_leaderboard_public), the caller always sees its own name** ([`c7bfccd7c`](https://github.com/urnetwork/server/commit/c7bfccd7cbe30a3fa043ad93003a02b8c12c8d45))
- **test: make stream p2p ice hermetic** ([`a1b216b56`](https://github.com/urnetwork/server/commit/a1b216b56ec400f097e45c0cf8930c2b248a7e6b))

### proxy

urnetwork/proxy [`5de305d04...3c2b79c56`](https://github.com/urnetwork/proxy/compare/5de305d04ec5652dcb7e72e167998b16b1782c4c...3c2b79c56024268efb45133dbcfcb961e865892d)

- **test: stream filtered output promptly** ([`3c2b79c56`](https://github.com/urnetwork/proxy/commit/3c2b79c56024268efb45133dbcfcb961e865892d))

### localizations

urnetwork/localizations [`2542b03c3...d0c6b8054`](https://github.com/urnetwork/localizations/compare/2542b03c389c50e0a6c2c53bea8e856cf6404485...d0c6b8054d9151bdfd8583ed894ae3cc3781a5d4)

- **Instant account strings for android** ([`d0c6b8054`](https://github.com/urnetwork/localizations/commit/d0c6b8054d9151bdfd8583ed894ae3cc3781a5d4))
  Tag the seedphrase screen strings android already shares with apple and the site, and add the five it had hardcoded: the no-email explainer, the leave confirmation, Go Back, Stay and the copied toast.
- **Points leaderboard: the switch reveals the network name (every ranked network is listed, Anonymous until then)** ([`fb445f74d`](https://github.com/urnetwork/localizations/commit/fb445f74de11ae1e0888fda400949a768127951d))

---

_Filtered out of this changelog: 1 merge commit (both sides are already listed), 2 version-stamp commits written by all/run.sh on every release, 1 ungoogle re-tag commit written by all/run.sh. Re-run `all/changelog.py --from v2026.9.4-1037517570 --to v2026.9.4-1037574650 --filters=` to include them._
